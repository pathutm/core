{
  "name": "youtube-s3-api",
  "version": "1.0.0",
  "description": "YouTube to S3 uploader",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "bash install-yt-dlp.sh && node index.js",
    "postinstall": "apt-get update && apt-get install -y yt-dlp"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.803.0",
    "dotenv": "^16.5.0",
    "express": "^4.21.2",
    "uuid": "^11.1.0",
    "ytdl-core": "^4.11.5"
  }
}
